describe('GET / events', () => {
  it('should respond with status 200 when there is data', () => {});
  it('should respond with status 404 when there is no data', () => {});
  it('should return all elements of the data file', () => {});
});

describe('POST / event', () => {
  it('should return status 201 when creation completed successfully', () => {});
  it('should return the id of the new element after creation completed successfully', () => {});
});
